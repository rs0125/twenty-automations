node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Local-only secrets (never commit)
crm-ec2-key.txt
*.pem

# SQL files kept local (contain operational secrets / DB-only logic)
sql/
